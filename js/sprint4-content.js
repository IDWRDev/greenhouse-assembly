/**
 * Legacy content injector retained as a compatibility no-op.
 *
 * Earlier site prototypes used this file to append provisional cards and
 * launch-status language at runtime. Canonical pages now contain their final
 * editorial structures directly in HTML, so injecting preview content would
 * create duplicate sections and reintroduce outdated ministry language.
 */
(() => {
  'use strict';
})();
