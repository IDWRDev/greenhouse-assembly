# The GreenHouse Assembly Ministries

Static GitHub Pages website. The production entry point is `index.html`; active styles and behavior live in `css/style.css` and `js/main.js`.

The public content deliberately marks unverified schedules, resources, online giving, and subscriptions as unavailable until ministry-approved details are supplied.
